// Declare two variables and assign them numeric values
let num1 = 5;
let num2 = 8;

// Outer "if" statement to check if num1 is greater than 0
if (num1 > 0) {
  // Inner "if" statement to check if num2 is greater than 0
  if (num2 > 0) {
    console.log("Both numbers are positive.");
  }
}