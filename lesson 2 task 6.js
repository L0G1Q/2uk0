// Declare a variable and assign it a numeric value
let number = 8;

// Check if the value is between 5 and 10 (inclusive)
if (number >= 5 && number <= 10) {
  console.log("The number is in the range of 5 to 10.");
} else {
  console.log("The number is outside the range.");
}